import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // TypeScript errors must be fixed before deployment
  // Removed ignoreBuildErrors to catch type errors during build
};

export default nextConfig;
